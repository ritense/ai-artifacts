# gzac-issues#901 — Lay-out of tasks, improvement in UX

https://github.com/generiekzaakafhandelcomponent/gzac-issues/issues/901

Rendered 2026-09-13 from `valtimo-platform/valtimo` at `6ca20a0fa`, against a locally running dev stack
(`auto-assign-test` case definition, seed data only). Every image is the same screen with the same data —
only the task panel differs.

| File | What it shows |
|------|---------------|
| `current.png` | The situation the ticket describes: `Taken` as a 24px heading in a fixed 412px right-hand panel |
| `option-a.png` | **A** — the `Taken` heading reduced to the 16px panel-header scale the start-form panel already uses |
| `option-b.png` | **B** — the panel collapsed to a 56px rail by default, with an open-task count |
| `option-c.png` | **C** — tasks moved into a tab next to the content tabs; default view, content full width |
| `option-c-tab-open.png` | **C** — the same, with the `Taken` tab selected |
